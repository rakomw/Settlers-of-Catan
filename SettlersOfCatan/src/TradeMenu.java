public class TradeMenu extends Menu {
  //player calls menus as he pleases, return their choice of commands in HumanPlayers functions

  //has to call trade in both players depending on what commodities are being traded
  //both functions will return if they have the commodities needed to make the trade
  //pass an array of ints on whats being gained/lost
}
